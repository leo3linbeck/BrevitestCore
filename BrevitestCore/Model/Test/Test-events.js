model.Test.patientInfo.onGet = function() {	return '';//	return ('ID: ' + this.patientNumber + '; ' + this.prescription.patientGender + '; DOB: ' + this.prescription.patientDOB.toDateString().substring(4));};model.Test.isComplete.onGet = function() {	return (this.finishedOn !== null && this.result !== null);};model.Test.rawData.onGet = function() {	var c = this.cartridges.find('failed === false');	return (c ? c.rawData : null);};model.Test.result.onGet = function() {	var data, i, indx, num_readings, reading, r_minus_b;		if (this.rawData) {		data = this.rawData.split('\n');		for (i = 2; i < data.length; i += 1) {			if (data[i] === '99') {				indx = i + 1;				break;			}		}		num_readings = 2 * parseInt(data[0].split('\t')[4]);		if (num_readings === indx) {			return 0;		}				r_minus_b = 0;		for (i = indx; i < num_readings; i += 1) {			reading = data[i].split('\t');			r_minus_b += parseInt(reading[4]) - parseInt(reading[6]);		}		return (r_minus_b / (num_readings - indx));	}	else {		return null;	}};